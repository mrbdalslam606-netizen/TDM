<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TdmApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TDM"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Download Engine Foreground Service (main process) -->
        <service
            android:name=".service.DownloadForegroundService"
            android:exported="false"
            android:stopWithTask="false" />

        <!-- Watchdog lives in its own process so it survives engine process death -->
        <service
            android:name=".service.WatchdogService"
            android:exported="false"
            android:process=":watchdog"
            android:stopWithTask="false" />

        <!-- Boot recovery: schedules an exact alarm (BOOT receivers cannot start FGS on Android 12) -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Exact alarm receiver — runs health checks & engine restart (FGS-start exempt on Android 12) -->
        <receiver
            android:name=".service.WatchdogAlarmReceiver"
            android:exported="false"
            android:process=":watchdog" />

        <!-- Engine control actions receiver (notification buttons) -->
        <receiver
            android:name=".service.EngineActionReceiver"
            android:exported="false" />

        <!-- Network change receiver -->
        <receiver
            android:name=".service.NetworkChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- Shizuku provider (optional reliability layer) -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- SAF: user-selected download root, persisted -->
        <!-- handled at runtime via ACTION_OPEN_DOCUMENT_TREE -->
    </application>

</manifest>
